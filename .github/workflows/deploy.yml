name: CI/CD with Docker and VerceL

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: kevinr08
          password: Kevincraft54

      - name: Build Docker image
        run: docker build -t kevinr08/frontend .

      - name: Push Docker image to Docker Hub
        run: docker push kevinr08/frontend

deploy_production:
  stage: deploy
  only:
    - main
  script:
    - npm install --global vercel
    - git clone https://github.com/tu-usuario/tu-repositorio.git
    - cd tu-repositorio
    - vercel pull --yes --environment=production --token=38KtTfoizDDgXsbgKeqO44tf
    - vercel build --prod --token=38KtTfoizDDgXsbgKeqO44tf
    - vercel deploy --prebuilt --prod --token=38KtTfoizDDgXsbgKeqO44tf


